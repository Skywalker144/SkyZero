# Shared paths used by every script (run.sh, elo.sh, play.sh, play_web.sh,
# bench.sh, and internal/{selfplay,shuffle,train,export,selfplay_daemon}.sh).
# The calling script must set ROOT (= repo root) before sourcing.
#
# Precedence: env var DATA_DIR (if already set) > value below > $ROOT/data.
# To switch experiments, either edit the line below or run with
#   DATA_DIR=/abs/path bash scripts/<script>.sh

DATA_DIR="${DATA_DIR:-$ROOT/data_b10c256_b19_renju_run_final}"
# DATA_DIR="${DATA_DIR:-$ROOT/data_b3c64_b17_renju_run3_test_ols}"
