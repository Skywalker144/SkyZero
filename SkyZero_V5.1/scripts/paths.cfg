# Shared paths used by every script (run.sh, run_selfplay_daemon.sh,
# selfplay.sh, shuffle.sh, train.sh, export.sh, elo.sh, play.sh, play_web.sh,
# ab.sh). The calling script must set ROOT (= repo root) before sourcing.
#
# Precedence: env var DATA_DIR (if already set) > value below > $ROOT/data.
# To switch experiments, either edit the line below or run with
#   DATA_DIR=/abs/path bash scripts/<script>.sh

# V5.1 PUCT run1: continues directly from V5 run4 (warm replay buffer + state).
# V5.1 PUCT writes new selfplay into the same dir, so the replay window blends
# V5's Gumbel-shaped rows with new PUCT rows until the older rows slide out.
DATA_DIR="${DATA_DIR:-/home/sky/RL/SkyZero/SkyZero_V5/data_b10c128_b17_renju_run4}"
# DATA_DIR="${DATA_DIR:-$ROOT/data_b10c128_b17_renju_puct_run1}"  # fresh-buffer alt
